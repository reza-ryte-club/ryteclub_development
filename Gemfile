source 'https://rubygems.org'
ruby '2.1.2'
gem 'rails_12factor', group: :production

# Use sqlite3 as the database for Active Record
#gem 'mongoid'
gem 'devise'
gem 'pg'
gem "socialization" #for follow unfollow feature
# Use SCSS for stylesheets
gem 'public_activity' # for homepage feed
gem 'koala'

gem 'rails', '4.1.5'
gem 'sprockets', '2.11.0'
gem 'omniauth-twitter'
gem 'omniauth-facebook'
gem 'bootstrap-sass', '3.2.0.0'
gem 'carrierwave'
gem 'fog','1.23.0'
gem 'font-awesome-sass'
gem 'ckeditor'
gem 'mini_magick'
gem 'jquery-fileupload-rails'
gem 'newrelic_rpm' #heroku new relic

#for truncate operation
#gem 'carrierwave_direct'
#gem 'sidekiq'
# Use sqlite3 as the database for Active Record
#gem 'angularjs-rails'
# environment configuration gem figaro
gem 'figaro'
# Use SCSS for stylesheets
# high_voltage gem to create static pages
gem 'high_voltage'

gem 'sass-rails'
gem 'autoprefixer-rails'

gem 'uglifier', '>= 1.3.0'
# Use CoffeeScript for .js.coffee assets and views
gem 'coffee-rails', '~> 4.0.0'

# See https://github.com/sstephenson/execjs#readme for more supported runtimes
# gem 'therubyracer', platforms: :ruby

# Use jquery as the JavaScript library
gem 'jquery-rails'



# Turbolinks makes following links in your web application faster. Read more: https://github.com/rails/turbolinks
gem 'turbolinks'

# Build JSON APIs with ease. Read more: https://github.com/rails/jbuilder
gem 'jbuilder', '~> 1.2'

group :doc do
  # bundle exec rake doc:rails generates the API under doc/api.
  gem 'sdoc', require: false
end

# Use ActiveModel has_secure_password
 gem 'bcrypt-ruby', '~> 3.1.2'
# html style mailer
  
group :development do
	gem 'better_errors'
end
# Use unicorn as the app server
# gem 'unicorn'

group :production do
  gem 'unicorn'
end

# Use Capistrano for deployment
# gem 'capistrano', group: :development

# Use debugger
# gem 'debugger', group: [:development, :test]
